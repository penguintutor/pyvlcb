site_name: PyVLCB
theme:
  name: material

# This plugin allows mkdocs to read Python docstrings
plugins:
  - search
  - mkdocstrings

name: Install dependencies
  run: |
     pip install -r docs/requirements.txt
     pip install .  # <--- Installs YOUR library in the CI environment
